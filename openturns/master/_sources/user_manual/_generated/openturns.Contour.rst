openturns.Contour
=================

.. currentmodule:: openturns

.. autoclass:: Contour

   
   .. automethod:: __init__

   
   .. rubric:: Methods

   .. autosummary::
   
      ~Contour.BuildDefaultPalette
      ~Contour.ConvertFromHSV
      ~Contour.ConvertFromHSVA
      ~Contour.ConvertFromHSVIntoRGB
      ~Contour.ConvertFromName
      ~Contour.ConvertFromRGB
      ~Contour.ConvertFromRGBA
      ~Contour.ConvertToRGB
      ~Contour.ConvertToRGBA
      ~Contour.GetValidColors
      ~Contour.GetValidFillStyles
      ~Contour.GetValidLineStyles
      ~Contour.GetValidPointStyles
      ~Contour.__init__
      ~Contour.buildDefaultLabels
      ~Contour.buildDefaultLevels
      ~Contour.clean
      ~Contour.draw
      ~Contour.getBoundingBox
      ~Contour.getCenter
      ~Contour.getClassName
      ~Contour.getColor
      ~Contour.getColorCode
      ~Contour.getData
      ~Contour.getDrawLabels
      ~Contour.getEdgeColor
      ~Contour.getFillStyle
      ~Contour.getId
      ~Contour.getLabels
      ~Contour.getLegend
      ~Contour.getLevels
      ~Contour.getLineStyle
      ~Contour.getLineWidth
      ~Contour.getName
      ~Contour.getOrigin
      ~Contour.getPalette
      ~Contour.getPaletteAsNormalizedRGBA
      ~Contour.getPattern
      ~Contour.getPointCode
      ~Contour.getPointStyle
      ~Contour.getRadius
      ~Contour.getShadowedId
      ~Contour.getTextAnnotations
      ~Contour.getTextPositions
      ~Contour.getTextSize
      ~Contour.getVisibility
      ~Contour.getX
      ~Contour.getY
      ~Contour.hasName
      ~Contour.hasVisibleName
      ~Contour.setCenter
      ~Contour.setColor
      ~Contour.setDrawLabels
      ~Contour.setFillStyle
      ~Contour.setLabels
      ~Contour.setLegend
      ~Contour.setLevels
      ~Contour.setLineStyle
      ~Contour.setLineWidth
      ~Contour.setName
      ~Contour.setOrigin
      ~Contour.setPalette
      ~Contour.setPattern
      ~Contour.setPointStyle
      ~Contour.setRadius
      ~Contour.setShadowedId
      ~Contour.setTextAnnotations
      ~Contour.setTextPositions
      ~Contour.setTextSize
      ~Contour.setVisibility
      ~Contour.setX
      ~Contour.setY
   
   

   
   
   .. rubric:: Attributes

   .. autosummary::
   
      ~Contour.thisown
   
   